PG_USER="postgres"
PG_HOST="localhost"
PG_DATABASE="booktracker"
PG_PASSWORD="your_password_here"
PG_PORT="5432"

# Production only — set to your deployed frontend URL to restrict CORS
# FRONTEND_URL="https://your-frontend-app.vercel.app"

# Set to "production" when deploying
# NODE_ENV="production"

# Used in production — set the full Postgres connection string
# DATABASE_URL="postgres://user:password@host:5432/dbname"
